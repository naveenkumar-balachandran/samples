apiVersion: v1
kind: Secret
metadata:
  name: iframefullserver-secrets
  namespace: boldbi-embed
  labels:
    app: iframefullserver
type: Opaque
stringData:
  # SECURITY WARNING: DO NOT commit real secrets to source control
  # Replace this placeholder at deploy time with the EXACT signing key from Bold UMS JWT settings
  jwt__signingkey: "REPLACE-WITH-BOLD-UMS-JWT-SIGNING-KEY"
